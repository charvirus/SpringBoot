{{>layout/header}}


<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/" role="button" class="btn btn-primary">목록</a>
        </div>
    </div>
    <br>
    <!--목록 출력 영역-->
    <table class="table table-horizontal table-bordered">
        <tr>
            <td>제목</td>
            <td>{{posts-view.title}}</td>
        </tr>
        <tr>
            <td>작성자</td>
            <td>{{posts-view.author}}</td>
        </tr>
        <tr>
            <td>내용</td>
            <td>{{posts-view.author}}</td>
        </tr>
    </table>
    <input type="hidden" id="id" name="id" value="{{posts-view.id}}">
    <button type="button" class="btn btn-primary"  onclick="location.href='/posts/update/{{posts-view.id}}' ">수정</button>
    <button type="button" class="btn btn-danger" id="btn-delete">삭제</button>
</div>

{{>layout/footer}}
